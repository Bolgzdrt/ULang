version: "3"
services:
  server:
    container_name: server
    build:
      context: ./server
    depends_on:
      - db
    volumes:
      - ./server:/usr/app
      - /usr/app/node_modules
    environment:
      # This might be a cosmos
      - DBURI=mongodb://db:27017/ulang
      - PORT=80
    ports: ['80:80']

  # I don't know about this part
  db:
    container_name: db
    image: mongo:4.0
    restart: always

  client:
    container_name: client
    build:
      context: ./client
    volumes:
      - ./client:/app
      - /app/node_modules
    ports:
      - '8080:8080'
    environment:
      # I don't know about this part
      - SERVER_URL="http://localhost/"
